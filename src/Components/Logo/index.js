import React, { Component } from 'react';

class Logo extends Component {
    render() {
        return (
            <div style={{ width: `${100}px`}}>
                Logo here
            </div>
        );
    }
}

export default Logo;
